% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{get_properties}
\alias{get_properties}
\title{Extract and Format Properties from a Schema}
\usage{
get_properties(schema, filter_ids = TRUE)
}
\arguments{
\item{schema}{List. The schema containing \code{properties} and \code{required} fields.}

\item{filter_ids}{Logical. Whether to filter out ID fields from the
properties. Defaults to \code{TRUE}.}
}
\value{
A character vector of formatted property names and indications of
required fields.
}
\description{
This function extracts property names from a schema, optionally filters out
ID fields, and applies formatting such as marking required fields and
prettifying the labels.
}
\examples{
\dontrun{
# Extract and format properties from a schema
formatted_props <- get_properties(schema)

# Extract properties without filtering ID fields
all_props <- get_properties(schema, filter_ids = FALSE)
}

}
